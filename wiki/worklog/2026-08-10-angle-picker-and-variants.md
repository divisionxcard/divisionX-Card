---
type: worklog
date: 2026-08-10
tags: [marketing, angles, variants, multi-platform, gemini, quota]
commits: [ab28188, adffdae, 84f483e]
status: ✅ โค้ดครบทั้ง 2 ข้อ — ข้อ 2 ยังไม่ได้ทดสอบกับ Gemini จริง (โควตาหมด) ทดสอบด้วย Ollama แทน
---

# เลือกมุมเล่าได้ + เรื่องเดียวเขียนหลายช่องทาง

เจ้าของสั่ง *"เริ่มทำทั้งหมดเรียงไปทีละข้อเลย"* — ทำข้อ 1-2 เสร็จ
ข้อ 3 (แก้โปสเตอร์) แยกให้ทีมงานทำขนานไปเพราะคนละไฟล์ ไม่ชนกัน

## ข้อ 1 · เลือกมุมเล่าได้จากการ์ดไอเดีย

migration 063 + ตัวคิดมุมทำไปแล้วเมื่อวาน **แต่ข้อมูลอยู่ใน DB เฉย ๆ หน้าเว็บยังโชว์ angle เดิม**
= ฟีเจอร์ที่ผู้ใช้กดไม่ถึง ซึ่งเท่ากับยังไม่มีอยู่จริง — บทเรียนเดียวกับที่เคยพลาดเรื่องปุ่มโปสเตอร์
([[2026-08-08-poster-builder]]) ครั้งนั้นเจ้าของกดปุ่มผิดอยู่ 2 รอบเพราะทางที่ใช้ได้ไม่มีปุ่ม

- การ์ดไอเดียแสดง `angles` เป็นปุ่มเลือก พร้อม brief อธิบายว่าจะเล่าอะไร
- ส่ง label ที่เลือกไปกับ `action=pick` → route หา brief ของมุมนั้นมาเป็นโจทย์ให้ AI
- บันทึก `chosen_angle` (เขียนแยก try เผื่อยังไม่ได้รัน migration)
- ไอเดียเก่าที่ไม่มี `angles` ยังโชว์ `angle` เดิม ไม่พัง

**ทดสอบด้วยข้อมูลจริง** — ไอเดีย "OP-12 ยอดโต 50%" เลือกคนละมุมได้โจทย์คนละอันจริง:
| มุม | โจทย์ที่ AI ได้ |
|---|---|
| เบื้องหลังตู้กด | เล่ามุมทีมงานเปิดดูตัวเลขหลังบ้าน |
| ตั้งคำถามชวนคุย | ถามสาวกว่าใบไหนเด็ด |
| ของเริ่มขาด เช็กด่วน | เตือนด้วยสถิติ 68 ซอง 12,268 บาท |

## ข้อ 2 · เรื่องเดียวเขียนได้หลายช่องทาง

เดิม 1 แถว = 1 ช่องทาง อยากลง FB + IG + TikTok ต้องกด 3 รอบ **แล้วก็ได้แคปชั่น
เดียวกันเป๊ะเพราะโจทย์เดียวกัน** — ซึ่งผิด แต่ละช่องมีธรรมเนียมคนละแบบ

### 2 การตัดสินใจเชิงออกแบบ
**เก็บรวมในแถวเดิม (`variants jsonb`) ไม่แยก 4 แถว** — วงจรอนุมัติเราคือ
"1 การ์ด = 1 เรื่อง" แยกแถวจะกลายเป็น 4 การ์ดที่ต้องอนุมัติซ้ำ 4 รอบทั้งที่เป็นเรื่องเดียวกัน

**ยิงครั้งเดียวให้เขียนครบทุกช่อง ไม่ยิงแยกช่องละครั้ง** เพราะ
1. โมเดลเห็นทุกช่องพร้อมกันถึงจะบังคับ "ห้ามเปิดประโยคเหมือนกัน" ได้จริง
2. Gemini free tier ติดลิมิตต่อนาที ยิง 3 ครั้งชนง่ายมาก

## 🔴 โควตา Gemini หมดกลางงาน — และสิ่งที่ได้เรียนรู้

รันเติมมุมย้อนหลังได้ 8/86 แล้วชน 429 · ลองใหม่วันถัดไปก็ยังชน
(Gemini รีเซ็ตตามเวลา Pacific ไม่ใช่เวลาไทย)

**ผลคือทดสอบข้อ 2 กับ Gemini จริงไม่ได้เลย** — ต้องใช้ Ollama บนเครื่องแทน

และเจอว่า **ปุ่มที่พึ่ง AI ทุกปุ่มตายหมดทั้งที่มี Ollama อยู่บนเครื่องและใช้งานได้**
→ เพิ่ม Ollama เข้า route แล้วให้ **ถอยไปใช้เองอัตโนมัติเมื่อ Gemini คืน 429**
(ยกเว้นตอนบังคับ provider ด้วย `AI_PROVIDER` — เคารพคำสั่ง ไม่แอบเปลี่ยน)

**บทเรียน: ระบบที่พึ่ง free tier ต้องมีทางถอยเสมอ** ไม่ใช่รอให้คนมากดสลับเอง
ตอนออกแบบคิดว่าโควตาไม่มีทางหมด — ผิด และผิดซ้ำสองครั้งในงานเดียวกัน
(ครั้งแรกคิดว่าลิมิตคือรายวัน จริง ๆ คือรายนาที)

## ⚠️ สิ่งที่ยังพิสูจน์ไม่ได้
ทดสอบ prompt ด้วย Ollama — **โครงใช้ได้** 3 ช่องต่างกันที่รูปแบบจริง
```
ig     174 ตัวอักษร  ยาวกลาง มีแฮชแท็ก
tiktok 101 ตัวอักษร  สั้นมากตามธรรมเนียม
script 279 ตัวอักษร  เขียนเป็นช็อต 1-4 พร้อมคำบรรยาย
```

**แต่ IG กับ TikTok เปิดประโยคเหมือนกันเป๊ะ** ("OP16 มาแล้ว!") ทั้งที่ใส่กฎห้ามไว้
qwen ไม่ทำตามกฎข้อนี้ · **ยังไม่รู้ว่า Gemini จะทำได้ไหม**

ถ้า Gemini ก็ไม่ทำตาม ต้องเปลี่ยนวิธี — เช่นบอกประโยคเปิดของช่องก่อนหน้าให้มันเลี่ยง
แบบเดียวกับที่ใช้ได้ผลในตัวกันแคปชั่นซ้ำ ([[2026-08-08-content-anti-repeat]])
คือ "ให้เห็นของที่ห้ามซ้ำ" ได้ผลกว่า "สั่งว่าอย่าซ้ำ"

## ค้างไว้
- [ ] ทดสอบข้อ 2 กับ Gemini จริงเมื่อโควตารีเซ็ต — โดยเฉพาะเรื่องประโยคเปิดซ้ำ
- [ ] เติมมุมที่เหลือ 78 ชิ้น
- [ ] ข้อ 3 แก้โปสเตอร์ 3 จุด (ทีมงานทำอยู่)
- [ ] ใส่ `idea_angles.py` เข้า workflow (ต้องเพิ่ม GEMINI_API_KEY เป็น secret)

## เกี่ยวข้อง
[[2026-08-09-idea-angles]] · [[2026-08-08-content-anti-repeat]] · [[2026-08-08-poster-builder]]
