---
type: worklog
date: 2026-08-08
tags: [marketing, poster, playwright, design, thai]
commits: [b3f37bd, ff6c99a, 5cb4a9f]
status: ✅ เรนเดอร์ได้จริง ไทยถูก 100% — ยังไม่ได้ต่อ workflow + ปุ่มบนเว็บ · รอความเห็นดีไซน์
---

# ตัวสร้างโปสเตอร์ — ทาง C ลงมือจริง

## ทำไมเลือก Playwright ไม่ใช่ Vercel
เจ้าของบอกว่า *"อยากได้ภาพที่สวยและถูกต้องที่สุด เวลารอไม่ใช่ปัญหา"*
→ เอา **Chromium เต็มตัวบน GitHub Actions** ไม่ใช่ `@sparticuz/chromium` ที่ตัดทอนมา
(build นั้นมีประวัติปัญหาเรื่องฟอนต์ ซึ่งเป็นจุดที่เราแพ้ไม่ได้พอดี)
แถม Playwright มีในโปรเจกต์อยู่แล้ว — ไม่ต้องเพิ่ม dependency ใด ๆ

## หลักที่ยึด: ตัวเลขทุกตัวมาจาก DB
```
จำนวนสาขา  ← นับจาก machines ที่ status=active จริง (ตอนนี้ 13)
รูปสินค้า   ← image_url จริงของ SKU ใน Storage
แคปชั่น     ← ที่คนอนุมัติแล้ว
AI          ← ทำได้แค่พื้นหลัง ไม่ได้เขียนอะไรลงในภาพ
```
กันปัญหาแบบโปสเตอร์ตัวอย่างจาก ChatGPT ที่โมเดลแต่งราคาการ์ดขึ้นมาเอง
(15,000 / 9,000 / ... ซึ่งเราไม่มีข้อมูลนั้นในระบบเลย)

## ไฟล์
- `deploy/tasks/poster_template.html` — ดีไซน์ล้วน แก้หน้าตาได้ไม่ต้องแตะ Python
- `deploy/agents/poster_render.py` — ดึงข้อมูล → ประกอบ HTML → screenshot → อัป Storage

## 🐛 บั๊กที่เจอจากการเรนเดอร์จริง (ไม่ใช่จากการอ่านโค้ด)

### 1. พาดหัวตัดกลาง "One Piece"
```
นาทีนี้การ์ด One
Piece ไม่ใช่แค่เกม
```
ตัดตามจำนวนตัวอักษรแล้วถอยหาช่องว่างที่ใกล้ที่สุด — **ใช้กับภาษาไทยไม่ได้
เพราะไทยไม่เว้นวรรคระหว่างคำ** ช่องว่างที่หาเจอจึงมักเป็นช่องว่างในชื่อภาษาอังกฤษพอดี

แก้เป็นตัดที่ **จุดจบประโยค (! ?)** ซึ่งเป็นขอบเขตความหมายจริง
ไม่มีก็ปล่อยทั้งท่อนแล้วย่อฟอนต์ตามความยาว (82/74/64/56) ดีกว่าตัดผิดที่

### 2. ไฮไลต์สีเลือกคำมั่ว
เน้น 2 คำท้ายได้ **"OP16 ที่"** — ไม่ใช่หน่วยความหมาย แถมคร่อมบรรทัด
เปลี่ยนเป็นเน้นเฉพาะ **ตัวเลขและรหัสชุด** (`5`, `OP16`) ซึ่งสายตาจับอยู่แล้วโดยธรรมชาติ

### 3. อื่น ๆ
- กลางภาพโหว่เมื่อไม่มีรูปสินค้า → หุบพื้นที่แล้วขยายเนื้อความแทน
- แถบท้ายตัด "ของแท้ 100%" เป็น 2 บรรทัด → `white-space: nowrap`
- เส้นสายฟ้าตัดกันกลางภาพเป็นกากบาทใหญ่แย่งสายตา → ย้ายลงมุม หรี่จาก .16 เป็น .10

## ⚠️ ดักไว้ล่วงหน้า — อีโมจิ
ตัดอีโมจิทิ้งก่อนเรนเดอร์ เพราะ ubuntu ของ GitHub Actions **ไม่มีฟอนต์อีโมจิสี**
จะออกมาเป็นสี่เหลี่ยม · บนเครื่อง Windows ไม่เห็นปัญหาเลย ถ้าไม่ดักไว้จะไปเจอ
ตอนรันจริงบน CI ซึ่งไกลจากจุดที่แก้ได้เร็ว

## ผลที่ได้
เรนเดอร์คอนเทนต์ #15 และ #16 ทั้งแบบมีรูปสินค้าและไม่มี
**ตัวอักษรไทยถูกทุกตัว** รวมเคสที่ Satori พัง (ที่ · สั่น · ผู้)
รูปซอง OP-15 / OP-16 เป็นของจริงจาก Storage · `13 สาขา` มาจาก DB

## 🔴 UX ที่ทำพลาด — เจ้าของเจอแต่ปุ่มที่ใช้ไม่ได้

เจ้าของเลือก *"ขอลองแบบไม่เปิด billing ก่อน"* แล้วรายงานกลับมาสองรอบว่า
ยังขึ้น **"สร้างภาพไม่ได้ — บัญชี Gemini ยังไม่ได้เปิด billing"**

สาเหตุคือผมเอง: บนการ์ดมีปุ่มภาพอยู่**ปุ่มเดียว** และเป็นปุ่มพื้นหลัง AI ที่ต้องเปิด billing
ส่วนตัวสร้างโปสเตอร์ที่**ทำงานฟรีอยู่แล้ว ยังไม่มีปุ่ม** ต้องรันจากคอมมานด์ไลน์
เจ้าของจึงไม่มีทางเข้าถึงทางที่ใช้ได้เลย ทั้งที่ผมบอกว่า "ลองได้ไม่จำกัด"

**บทเรียน: ฟีเจอร์ที่ผู้ใช้กดไม่ถึง = ยังไม่มีอยู่จริง**
บอกว่า "ค้างแค่ปุ่ม" ฟังดูเหมือนงานเล็ก แต่สำหรับคนใช้มันคือทั้งหมด

แก้แล้ว:
- ปุ่ม **"สร้างโปสเตอร์"** เป็นปุ่มหลัก (น้ำเงินเต็ม)
- ปุ่มพื้นหลัง AI ลดเป็นลิงก์จาง และเขียนในปุ่มเลยว่า **"ต้องเปิด billing"**
  → ไม่ให้กดแล้วไปเจอ error โดยไม่รู้ตัว
- แถบแจ้งเตือนงาน async + ปุ่มรีเฟรช เพราะงานใช้เวลา 1-2 นาที ถ้าเงียบคนจะกดซ้ำ

## ค้างไว้
- [ ] พื้นหลัง AI — โค้ดรองรับแล้ว รอเปิด billing Gemini
- [ ] **รอความเห็นดีไซน์จากเจ้าของ** — ที่ทำเป็นแนวสะอาด-พรีเมียม
      ส่วนของ ChatGPT ที่เจ้าของชอบจัดจ้านกว่ามาก (พาดหัวเอียง เงาทอง กรอบการ์ด)
      ปรับได้ที่ `poster_template.html` ล้วน

## เกี่ยวข้อง
[[2026-08-08-thai-text-rendering]] · [[2026-08-08-content-image-gen]] · [[reference_brand_visual_identity]]
