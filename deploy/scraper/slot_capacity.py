"""เพดานความจุช่องซองของ **ตู้ Payif เท่านั้น** — ไม่ใช่กฎรวมของทุกยี่ห้อ

ทำไม (เจ้าของแจ้ง 31 ส.ค. 2026):
    ตู้ Payif เป็นตู้คนละแบบกับ VMS/WorldWide — ช่องซองของมันใหญ่กว่า จุได้ 15 ซอง
    แต่เราตั้งนโยบายว่าจะเติมแค่ 12 ซองต่อช่อง
    ใบจัดของคิดจาก ความจุ − คงเหลือ จึงบวกเกินช่องละ 3 ซองทุกวัน

    วัดจากของจริงวันเดียวกัน: pf01 มีช่องที่รายงาน 15 อยู่ 45 ช่อง และเกือบทุกช่อง
    เหลือ 12 อยู่แล้ว → ใบจัดของสั่งขนเกินไปวันละ ~135 ซอง (153 → 18 หลังแก้)

⚠️ **ห้ามเอาไปใช้กับ scraper ยี่ห้ออื่น** — เจ้าของกำชับตอนรีวิวรอบแรกว่าเรื่องนี้เป็นของ
   แบบตู้ Payif โดยเฉพาะ · VMS/WorldWide ให้ใช้ความจุที่ตู้รายงานตรง ๆ
   รอบแรกใส่ไว้ทั้งสามยี่ห้อด้วยเหตุผลว่า "วันนี้ไม่มีตู้อื่นเกิน 12 อยู่แล้ว จึงไม่กระทบ"
   ซึ่งจริงเฉพาะวันนี้ — วันที่ตู้ VMS/WW รุ่นใหม่มีช่องใหญ่กว่า 12 กฎจะไปตัดมันเงียบ ๆ
   แล้วใบจัดของจะสั่งของ**ขาด** ซึ่งเจ็บกว่าสั่งเกิน (ลูกค้าไปถึงแล้วของหมด)

ทำไมตัดตั้งแต่ตอนเขียนลง DB ไม่ใช่ตอนแสดงผล:
    machine_stock.max_capacity ถูกอ่านจากหลายที่มาก — หน้าเตรียมของเติม ·
    หน้าสต็อกหน้าตู้ · เตือนของใกล้หมด · slot_tracking · ข้อความ Telegram
    (ทั้งฝั่ง JS และ Python) ถ้าตัดที่ปลายทางต้องแก้ทุกที่ แล้วจะลืมที่ใดที่หนึ่งเสมอ
    — โปรเจกต์นี้เคยเจ็บกับ SKU mapper ที่ถูกก๊อปไป 6 ไฟล์มาแล้ว
    เก็บเลขเดียวตั้งแต่ต้นทาง ทุกคนที่อ่านจึงเห็นตรงกันโดยไม่ต้องรู้กฎนี้

⚠️ กล่องไม่โดนเพดานนี้ — คนละหน่วยกัน (ช่องกล่องจุ 4 กล่อง = 96 ซอง)
   เพดาน 12 เป็นเรื่องของ "ซอง" เท่านั้น
   กติกาแยกกล่อง/ซองใช้ชื่อสินค้าแบบเดียวกับฝั่งเว็บ (PageRefillPrep.jsx: ชื่อมีคำว่า box)

ใช้ที่เดียว: deploy/scraper/payif_stock_sync.py
"""

PACK_SLOT_MAX = 12


def is_box(product_name):
    """ช่องนี้เป็นสินค้าแบบกล่องไหม — ดูจากชื่อ เหมือนที่หน้าเว็บใช้"""
    return "box" in (product_name or "").lower()


def plan_capacity(reported, product_name=None):
    """ความจุที่เอาไปคิดยอดเติม · ซองไม่เกิน PACK_SLOT_MAX · กล่องใช้ตามที่ตู้รายงาน"""
    cap = int(reported or 0)
    return cap if is_box(product_name) else min(cap, PACK_SLOT_MAX)


def find_overfilled(slots, cap=PACK_SLOT_MAX):
    """ช่องซองที่ "ของจริงหน้าตู้" มีมากกว่าเพดานที่เราตั้งไว้ — เจ้าของขอให้เตือนทันที

    ทำไมต้องรู้: เราตัด max_capacity เหลือ 12 ไปแล้ว ถ้าหน้าตู้จริงมี 15
    ระบบจะคิดว่าช่องนั้น "เต็มแล้ว" (เติม = 12 − 15 → ติดลบ → ปัดเป็น 0) แล้วเงียบไปเลย
    ทั้งที่ของหน้าตู้ไม่ตรงกับที่เราตั้งไว้ และสต็อกในระบบก็จะเพี้ยนตามไปด้วย

    ⚠️ ต้องดูจาก remain ที่ตู้รายงาน เทียบกับ "เพดาน" ไม่ใช่เทียบกับ max_capacity
       ที่เราตัดแล้ว — เทียบกับตัวที่ตัดแล้วก็ยังเจอ แต่ถ้าวันหลังใครเปลี่ยนไปเก็บ
       ความจุดิบลง DB ด่านนี้จะเงียบทันทีโดยไม่มีใครรู้ · ผูกกับค่าคงที่ปลอดภัยกว่า

    slots: dict ที่มี product_name · remain · slot_number (จะมี reported_capacity ด้วยก็ได้)
    """
    over = []
    for s in slots:
        name = s.get("product_name")
        if is_box(name):
            continue                       # กล่องคนละหน่วย ไม่อยู่ใต้เพดานซอง
        remain = int(s.get("remain") or 0)
        if remain <= cap:
            continue
        over.append({
            "slot_number": s.get("slot_number"),
            "product_name": name,
            "sku_id": s.get("sku_id"),
            "remain": remain,
            "reported_capacity": s.get("reported_capacity"),
            "over": remain - cap,
        })
    return over
