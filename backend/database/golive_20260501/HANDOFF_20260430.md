# คู่มือ Go-Live 1 พ.ค. 2026 — สำหรับเครื่องใหม่

📅 เตรียมไว้ 30 เม.ย. 2026 เย็น · อ่านบนเครื่องใหม่ก่อนเริ่มงาน

> **วิธีใช้คู่มือนี้**: ทำตาม checkbox · กล่อง code สีเทาที่ขึ้นต้นด้วย 🤖 = copy-paste ส่งให้ Claude ทำให้ · ไม่ต้องพิมพ์เอง

---

## 🚀 เริ่มต้น — บอก Claude เครื่องใหม่ครั้งแรก

เปิด Claude Code ตรงโฟลเดอร์ที่ copy USB มา · paste ข้อความนี้:

> 🤖 "อ่าน HANDOFF_20260430.md ในโฟลเดอร์นี้ แล้วเช็คให้หน่อยว่าเครื่องใหม่พร้อมใช้งานไหม (Python, openpyxl, browser session) ถ้าขาดอะไรบอกวิธีติดตั้ง"

Claude จะเช็คให้อัตโนมัติ + แจ้งสิ่งที่ต้องติดตั้ง

---

## ✅ Pre-flight ก่อนวันงาน

- [ ] **Python 3** ติดตั้งแล้ว (ลอง `py --version`)
- [ ] **openpyxl** ติดตั้งแล้ว: `py -m pip install openpyxl`
- [ ] Browser login: GitHub (divisionxcard) + Supabase Dashboard (prod)
- [ ] LINE/email พร้อมรับ Excel จากแอดมิน
- [ ] โฟลเดอร์เปล่าสำหรับเก็บ backup CSV (เช่น `C:\backup\golive\`)

---

## 🟡 30 เม.ย. ดึก ~22:00 — ปิด GitHub Actions

**Manual action** (ทำเองในเว็บ):

1. เปิด https://github.com/divisionxcard/divisionX-Card/actions
2. sidebar ซ้าย → คลิก **"VMS Daily Sync"** → ปุ่ม `...` มุมขวาบน → **"Disable workflow"**
3. ทำซ้ำกับ **"VMS Stock Sync"**
4. **Verify**: sidebar ต้องโผล่ "Disabled workflows" group มีชื่อทั้ง 2

> `weekly-backup.yml` ไม่ต้องแตะ (รันแค่วันอาทิตย์)

---

## 🔴 1 พ.ค. — วันงาน Timeline

### ⏰ 07:00 — แอดมินเริ่มเติมตู้

**Manual action:**
- [ ] บอกแอดมิน: เติมจาก User stock ของตัวเอง · เติมตู้ทุกตู้ให้เต็ม
- [ ] ติดป้าย "ปิดปรับปรุง 07:00-09:30" บนตู้
- [ ] ห้ามขายก่อน 09:30

---

### ⏰ 08:30 — ตรวจตู้เต็ม

- [ ] เดินตรวจ chukes01-04 ครบ 4 ตู้ · ทุกช่องเต็ม

---

### ⏰ 08:35 — Trigger VMS Sync

**Manual action:**
1. เปิด https://division-x-card.vercel.app/ → login ด้วย `aofwara66`
2. ไปหน้า **"สต็อกหน้าตู้"**
3. กดปุ่ม **"ดึงข้อมูล VMS"** → รอ ~1 นาที → กด Refresh
4. ตรวจ remain ของ slot ตรงกับของจริง (สุ่ม 2-3 ช่อง/ตู้)

---

### ⏰ 08:45 — รับ Excel จากแอดมิน

**Manual action:**
1. รอแอดมินส่ง `golive_template.xlsx` ที่กรอกแล้ว (LINE/email)
2. **Save** ในโฟลเดอร์ `golive_20260501\` ชื่อ `golive_filled_20260501.xlsx`

ให้ Claude เช็คให้ก่อนว่าแอดมินกรอกครบ:

> 🤖 "เปิด `golive_filled_20260501.xlsx` แล้วเช็คว่า Sheet Main_Stock + User_Stock กรอกครบไหม · ถ้าขาด SKU ไหนบอกด้วย"

---

### ⏰ 09:00 — Backup CSV (7 ตาราง)

**Manual action** ทำใน Supabase Dashboard:

1. เปิด https://supabase.com/dashboard/project/xethnqqmpvlpmafvphky/sql
2. **ตรวจ URL ว่ามี `xethnqqmpvlpmafvphky`** (prod)
3. เปิดไฟล์ `01_backup_export.sql` (ใน USB) — มี 7 blocks
4. **block ละ 1 ครั้ง**: paste → Run → กด **Export → CSV** → save เป็นไฟล์
5. ทำครบ 7 ไฟล์: `stock_in`, `stock_out`, `stock_transfers`, `claims`, `sales`, `machine_stock`, `login_history`
6. เก็บใน `C:\backup\golive\` (หรือโฟลเดอร์ backup ที่เตรียมไว้)

ถ้าขี้เกียจเปิดไฟล์เอง:

> 🤖 "เปิด `01_backup_export.sql` แสดง block ที่ 1 ให้หน่อย"

(เพื่อ copy-paste แต่ละ block)

---

### ⏰ 09:10 — Reset Transactional

**Manual action:**
1. เปิด `02_reset_transactional.sql` · copy ทั้งไฟล์
2. paste ใน Supabase SQL Editor (prod) → **Run**
3. ดู panel **"Notices"**:
   - ✅ ถ้าเห็น `Reset OK · machine_stock พร้อมใช้ (XX slots)` = ผ่าน
   - ⚠ ถ้าเห็น `WARNING machine_stock ว่าง` = กลับ step 08:35 trigger sync ก่อน

ถ้าขี้เกียจเปิดไฟล์:

> 🤖 "แสดง content ของ `02_reset_transactional.sql` ให้หน่อย"

ถ้า Reset แล้วไม่ขึ้น notice ที่คาด:

> 🤖 "Run reset แล้วเห็น notice แบบนี้ [paste notice] · มีปัญหาไหม?"

---

### ⏰ 09:15 — Convert Excel → SQL

**ให้ Claude run ให้:**

> 🤖 "Run `py 04_excel_to_sql.py golive_filled_20260501.xlsx` แล้วบอกผลด้วย ถ้า error ช่วยอ่าน error แล้วบอกวิธีแก้"

Claude จะ:
- รัน script
- ถ้าสำเร็จ → จะมีไฟล์ `03_seed_generated.sql` ใหม่
- ถ้า error (Excel ผิด format) → Claude บอกบรรทัดที่ผิดให้แก้ใน Excel

ทำเองได้เหมือนกัน:
```bash
cd path\to\golive_20260501
py 04_excel_to_sql.py golive_filled_20260501.xlsx
```

---

### ⏰ 09:20 — Seed Data

**Manual action:**
1. เปิด `03_seed_generated.sql` (เพิ่งถูกสร้าง) · copy ทั้งไฟล์
2. paste ใน Supabase SQL Editor → **Run**
3. ดู Notices: ต้องเห็น `Seed OK`

> 🤖 "แสดงสรุปของ `03_seed_generated.sql` ที่เพิ่งสร้าง — มีกี่ INSERT, รวม SKU กี่ตัว, total_cost รวมเท่าไหร่"

(เช็คเลขก่อน run ว่าตัวเลขดูสมเหตุสมผล)

---

### ⏰ 09:25 — Verify บนเว็บ

**Manual action** เปิด https://division-x-card.vercel.app/ (Refresh `Ctrl+Shift+R`):

- [ ] **Dashboard** — สต็อกรวม + ซื้อรวม ตรงกับ Excel
- [ ] **สต็อกหน้าตู้** — ทุกตู้มี slot
- [ ] **จัดการสต็อก** — SKU ทุกตัวมี avg_cost ถูกต้อง
- [ ] **คงเหลือในมือ** — ยอด User แต่ละคนตรง

---

### ⏰ 09:30 — Go-Live ⏯

- [ ] แจ้งแอดมิน: ระบบใช้งานได้แล้ว
- [ ] ลอกป้าย "ปิดปรับปรุง" ออกจากตู้
- [ ] ทดสอบ: เบิก SKU 1 ตัว User → Machine

---

### ⏰ 09:45 — เปิด GitHub Actions กลับ

**Manual action:**
1. https://github.com/divisionxcard/divisionX-Card/actions
2. คลิก workflow ที่ disabled → **"Enable workflow"**
3. กด **"Run workflow"** (trigger sync ทันที · ดึงยอดขายเช้าตู้)
4. ทำซ้ำกับอีก workflow

---

### ⏰ 10:00+ — เฝ้าระวัง

ทุก 30 นาที ดู Dashboard เทียบกับ VMS · ถ้าเจอ bug:

> 🤖 "เจอ bug แบบนี้บนเว็บ [อธิบาย/screenshot path] · ช่วยดูใน source code ว่าแก้ตรงไหน"

---

## 🆘 ถ้าพังกลางทาง

### ก่อน Step 09:20 (ก่อน Seed)

> 🤖 "ขอ rollback reset · เปลี่ยน COMMIT เป็น ROLLBACK ใน 02_reset_transactional.sql ให้หน่อย"

หรือ Import CSV backup กลับใน Supabase Dashboard → Table editor → Import

### หลัง Step 09:20 (Seed แล้ว แต่เจอปัญหา)

> 🤖 "Seed แล้วเจอปัญหา [อธิบาย] · ควร rollback ไหม หรือแก้ตรงไหนได้บ้าง"

3 ทางเลือก:
1. Run reset อีกครั้ง → import CSV backup กลับ
2. Run Convert + Seed ใหม่ (ถ้า Excel ผิด แก้แล้วรันใหม่)
3. แก้บนเว็บโดยตรง (ถ้า bug เล็ก)

### Error ที่ไม่เคยเจอ

> 🤖 "Run [step ที่ทำ] แล้วเจอ error นี้: [paste error] · บอกสาเหตุและวิธีแก้"

---

## 📞 ลิงก์สำคัญ

| ที่ | ลิงก์ |
|---|---|
| เว็บ prod | https://division-x-card.vercel.app/ |
| Supabase prod | https://supabase.com/dashboard/project/xethnqqmpvlpmafvphky/sql |
| GitHub Actions | https://github.com/divisionxcard/divisionX-Card/actions |

## 👤 Username admin

| ชื่อ | username | role |
|---|---|---|
| DivisionX Card | divisionxcard | admin |
| T | pornthep_sm1991 | admin |
| AOFSEN | aofwara66 | user |
| พี่ M | power23n | user |
| M-zadiz | mzadiz1989 | user |

(รหัสผ่านเดิม · ลืม → "ลืมรหัสผ่าน" + ใส่ username → ลิงก์ส่ง email)

---

## 📋 Backlog (ทำเมื่อสะดวก หลัง go-live)

- Claim #1 — auto-suggest refund (~30 นาที)
- Claim #2 — auto-add stock returned (1-2 ชม.)
- Claim #4 — ตัดสต็อก damaged/lost (1-2 ชม.)
- RLS Phase B-D
- Rule หน่วยเบิก ใน UI (Main = Cotton/Box เท่านั้น)
- Sell_price ใส่ในระบบ (ตอนนี้ = 0 ทุก SKU)
- ตู้ยี่ห้อใหม่ + API (รอ spec จากผู้ผลิต)

---

## 🎯 สรุป — บอก Claude แค่ประโยคเดียว

ถ้าจำ prompt ทั้งหมดไม่ไหว · ใช้ประโยคเดียวนี้พอ:

> 🤖 "เราอยู่ในช่วง [step เวลาที่ทำ] ของ HANDOFF_20260430.md ตอนนี้ [อธิบายสถานการณ์] · ขั้นตอนถัดไปต้องทำอะไรและช่วยทำให้ส่วนที่ทำได้"

Claude จะอ่าน doc เอง · บอก step ถัดไป · run คำสั่งให้
