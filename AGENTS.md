# AGENTS.md — จุดเริ่มต้นสำหรับ AI agent ทุกตัว

> ไฟล์นี้ตั้งใจให้สั้น **แหล่งความจริงคือ [CLAUDE.md](CLAUDE.md) — อ่านไฟล์นั้นก่อนเริ่มงานเสมอ**
> เดิมไฟล์นี้เคยเป็นสำเนาของ CLAUDE.md ทั้งไฟล์ แล้วข้อมูลเก่าค้าง (เคยเขียนว่า Sales API ถูก block
> ทั้งที่ใช้งานได้แล้ว) — agent ที่อ่าน AGENTS.md ก่อนจึงได้ข้อมูลผิด จึงเปลี่ยนมาเป็นตัวชี้ทางแทน

## สิ่งนี้คืออะไร

DivisionX Card — ธุรกิจตู้กดการ์ดสะสมอัตโนมัติในห้าง 13 ตู้ (กรุงเทพฯ + ปริมณฑล) 3 แบรนด์หลังบ้าน
คนละเจ้า (VMS `chukes*` · WorldWide `wwv*` · Payif `pf01`) repo นี้คือระบบหลังบ้านของเจ้าของธุรกิจ:
ดึงข้อมูลจากตู้ → เก็บลง Supabase → แสดงบนเว็บ Next.js + ช่วยงานการตลาด

## อ่านตามลำดับนี้

| ลำดับ | ไฟล์ | ได้อะไร |
|---|---|---|
| 1 | [CLAUDE.md](CLAUDE.md) | สถาปัตยกรรม · ไฟล์สำคัญ · ทุกหน้าเว็บ · จุดเฝ้าระวัง |
| 2 | [.claude/skills/](.claude/skills/) | คู่มือเฉพาะงาน — `dvx-db` ฐานข้อมูล · `dvx-web` หน้าเว็บ · `dvx-sku` สินค้า · `dvx-sync` ซิงค์ข้อมูล · `dvx-machine` ตู้ · `dvx-content` ระบบคอนเทนต์ |
| 3 | [wiki/worklog/](wiki/worklog/) | บันทึกงานย้อนหลัง — เจอปัญหาแปลก ๆ ให้ค้นที่นี่ก่อน มักเคยเจอมาแล้ว |

## เว็บอยู่ตรงไหน

```
deploy/                        Next.js App Router (deploy บน Vercel)
├── app/                       หน้าเว็บ + API routes
│   ├── api/marketing/         ระบบคอนเทนต์ (ideas · content · calendar · metrics)
│   └── marketing/             หน้าจัดการการตลาด
├── components/DivisionXApp.jsx  หน้าหลักทั้งหมดรวมอยู่ไฟล์เดียว (ไฟล์ที่แก้บ่อยสุด)
├── lib/supabase.js            ทุก query ที่หน้าเว็บใช้
├── agents/                    สคริปต์ Python — ชั้นข้อมูลกลางคือ dvx_data.py
├── scraper/                   ตัวดึงข้อมูลจากตู้ทั้ง 3 แบรนด์
└── mcp/dvx_mcp_server.py      เปิดข้อมูลให้ AI agent เรียกผ่าน MCP
```

## กฎที่ทำให้ไม่พัง

- **ตัวเลขต้องมาจากฐานข้อมูลจริงเสมอ** อย่าเดาจากที่จำได้ · PostgREST คืนแค่ 1000 แถวต่อครั้ง
  ถ้าไม่แบ่งหน้าจะได้ตัวเลขน้อยกว่าจริงแบบเงียบ ๆ (ดู `dvx-db`)
- **การ map ชื่อสินค้าอยู่ใน hardcode 6 ไฟล์** ไม่ใช่ตาราง แก้ไม่ครบ = ยอดขายหายทั้งแถว (ดู `dvx-sku`)
- **commit แล้วต้อง push** GitHub Actions รันจาก `origin/main` ถ้าไม่ push ตัวซิงค์จะรันโค้ดเก่า
  แล้วเขียนทับข้อมูลที่เพิ่งซ่อมด้วยมือ
- **`deploy/.env.local` มี service key ของ Supabase และ GitHub token** — อย่าพิมพ์ค่าออกมา
  อย่าคัดลอกไปที่อื่น อย่าส่งขึ้นแชท
